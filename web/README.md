This folder contains the web application for querying and viewing data.  It is a Flask application written in Python 3.

# Setup Instructions
for local development:
```
python3 -m venv env
env/bin/pip3 install -r requirements.txt
env/bin/flask run
```

